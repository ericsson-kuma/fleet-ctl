# fleet-ctl

A device-fleet control plane in Go + gRPC: device registration, telemetry
streaming, and staged config rollouts with SRE-style guardrails — canary
waves, health windows, and automatic rollback.

Status: under construction. The commit history builds the system up layer by
layer; the full README lands with the docs commit.
